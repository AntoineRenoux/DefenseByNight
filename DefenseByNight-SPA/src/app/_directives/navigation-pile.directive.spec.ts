/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { NavigationPileDirective } from './navigation-pile.directive';

describe('Directive: NavigationPile', () => {
  it('should create an instance', () => {
    const directive = new NavigationPileDirective();
    expect(directive).toBeTruthy();
  });
});
