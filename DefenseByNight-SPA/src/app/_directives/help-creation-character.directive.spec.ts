/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { HelpCreationCharacterDirective } from './help-creation-character.directive';

describe('Directive: HelpCreationCharacter', () => {
  it('should create an instance', () => {
    const directive = new HelpCreationCharacterDirective();
    expect(directive).toBeTruthy();
  });
});
